{% extends 'base.html.twig' %}

{% block body %}
    <aricle>
        <h4>{{article.title}}</h4>
        <p>{{article.category.title}}</p>
        <img src="{{article.image}}" alt="voire" style="margin: 0 auto;">
        <p>{{article.content}}</p>
        <a href="{{path('blog_edit',{'id':article.id})}}" class="btn btn-primary">Mettre à jour</a>
    </article>
    <section>
        {% for comment in article.comments %}
            <div id="comment">
                <div class="row">
                    <div class="col-3">
                        {{comment.author}}
                    </div>
                    <div class="col">
                        {{comment.content}}
                    </div>
                </div>
            </div>
            <br>
        {% endfor %}
        <div class="comment">
            {{form_start(formComment)}}
                {{form_row(formComment.content,{'attr':{'placeholder':'Titre de l\'article'}})}}
                <button type="submit" class="btn btn-success">Envoyer</button>
            {{form_end(formComment)}}
        </div>
    </section>
{% endblock %}